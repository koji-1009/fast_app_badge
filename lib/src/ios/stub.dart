/// (Stub) Requests permission to display badges on iOS.
void requestBadgePermission({
  required bool badge,
  required bool sound,
  required bool alert,
}) {}

/// (Stub) Updates the application icon badge number on iOS.
void updateBadge(int count) {}
